
#### 配置示例

```yaml
solon.cloud.aws.s3:
  file:
    enable: true                  #是否启用（默认：启用）
    bucket: "demo.bucket"
    regionId: "ap-southeast-1"
    accessKey: "ak..."
    secretKey: "sk..."
```