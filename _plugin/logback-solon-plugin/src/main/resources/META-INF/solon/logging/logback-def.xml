<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE configuration>
<configuration scan="true" scanPeriod="60 seconds" debug="false">
    <conversionRule conversionWord="tags" converterClass="ch.qos.logback.solon.SolonTagsConverter" />

    <solonProperty name="APP_NAME" source="solon.app.name"
                   defaultValue="solon" />

    <solonProperty name="FILE_LOG_NAME" source="solon.logging.logback.file.name"
                   defaultValue="logs/${APP_NAME}" />

    <solonProperty name="FILE_LOG_PATTERN" source="solon.logging.logback.file.pattern"
                   defaultValue="%-5level %d{yyyy-MM-dd HH:mm:ss.SSS} [-%thread][*%mdc{traceId}]%tags[%logger{20}]: %n%msg%n" />

    <solonProperty name="FILE_LOG_LEVEL" source="solon.logging.logback.file.level"
                   defaultValue="TRACE" />


    <solonProperty name="CONSOLE_LOG_PATTERN" source="solon.logging.logback.console.pattern"
                   defaultValue="%highlight(%-5level %d{yyyy-MM-dd HH:mm:ss.SSS} [-%thread][*%mdc{traceId}]%tags[%logger{20}]:) %n%msg%n" />

    <appender name="Console" class="ch.qos.logback.solon.SolonAppender" />

<!--    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">-->
<!--        <encoder>-->
<!--            <pattern>${CONSOLE_LOG_PATTERN}</pattern>-->
<!--        </encoder>-->
<!--    </appender>-->

    <appender name="File" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>${FILE_LOG_LEVEL}</level>
        </filter>

        <File>${FILE_LOG_NAME}.log</File>
        <append>true</append>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${FILE_LOG_NAME}_%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>7</maxHistory>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>100 MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <root>
        <level value="trace" />
        <appender-ref ref="Console"/>
        <appender-ref ref="File"/>
    </root>

</configuration>
