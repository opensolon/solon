# 企业微信号配置(必填)
wx.cp.corp-id : '123456'
wx.cp.corp-secret : 'weixin-cp-java-sdk'
# 选填
wx.cp.token : 'qazwsx'
wx.cp.aes-key : '12qwszx'
wx.cp.agent-id : '354'
# ConfigStorage 配置（选填）
wx.cp.config-storage.type: memory # memory 默认，目前只支持 memory,redisson 类型，可以自行扩展 redis 等类型
# http 客户端配置（选填）
wx.cp.config-storage.http-proxy-host: 30.124.163.18
wx.cp.config-storage.http-proxy-port: 7463
wx.cp.config-storage.http-proxy-username: 123
wx.cp.config-storage.http-proxy-password: qaszxc
# 最大重试次数，默认：5 次，如果小于 0，则为 0
wx.cp.config-storage.max-retry-times: 5
# 重试时间间隔步进，默认：1000 毫秒，如果小于 0，则为 1000
wx.cp.config-storage.retry-sleep-millis: 1000