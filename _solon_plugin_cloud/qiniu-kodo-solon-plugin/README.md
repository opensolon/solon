
#### 配置示例

```yaml
solon.cloud.qiniu.kodo:
  file:
    enable: true                  #是否启用（默认：启用）
    bucket: "demo-bucket"
    endpoint: "http://xxxx.demo.com"
    accessKey: "ak..."
    secretKey: "sk..."
```