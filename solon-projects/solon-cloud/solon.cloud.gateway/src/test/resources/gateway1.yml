server.port: 8900
server.gateway.port: 8901

solon.app:
  name: demo-gateway
  group: gateway

solon.cloud.gateway:
  routes:
    - target: "http://localhost:8080"
      predicates:
        - "Path=/demo/**"
      filters:
        - "StripPrefix=1"

solon.logging.logger:
  root:
    level: "INFO"