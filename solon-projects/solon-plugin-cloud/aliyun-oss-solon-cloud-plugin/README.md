
#### 配置示例

```yaml
solon.cloud.aliyun.oss:
  file:
    enable: true                  #是否启用（默认：启用）
    bucket: "demo-bucket"
    accessKey: "ak..."
    secretKey: "sk..."
    endpoint: "oss-cn-hangzhou.aliyuncs.com"
```